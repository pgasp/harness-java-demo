apiVersion: v1
kind: Service
metadata:
  name: harness-java-demo
  namespace: harness-demo
spec:
  selector:
    app: harness-java-demo
  ports:
    - protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
